apiVersion: v1
kind: Service
metadata:
  name: nudge-db
  labels:
    app.kubernetes.io/name: nudge
    app.kubernetes.io/component: db
spec:
  clusterIP: None
  selector:
    app.kubernetes.io/name: nudge
    app.kubernetes.io/component: db
  ports:
    - name: postgres
      protocol: TCP
      port: 5432
      targetPort: postgres
